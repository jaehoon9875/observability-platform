# provectus/kafka-ui 커스텀 설정
# chart version: 0.7.6 / app version: v0.7.2
# 역할: Kafka 클러스터 웹 UI

# 연결할 Kafka 클러스터 설정 (yamlApplicationConfig로 전달해야 chart가 인식)
yamlApplicationConfig:
  kafka:
    clusters:
      - name: kafka
        bootstrapServers: kafka-kafka-bootstrap:9092
